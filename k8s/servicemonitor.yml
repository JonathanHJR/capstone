apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: calorie-tracker
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: calorie-tracker
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
